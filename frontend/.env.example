# Base URL the Next.js frontend uses to reach the FastAPI backend.
# In docker-compose this is injected as http://localhost:8000/api/v1 (see docker-compose.yml).
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1